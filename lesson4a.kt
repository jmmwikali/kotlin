fun main() {
    // FUNCTIONS - They are blocks of statements that perform a given task.
    // A function is introduces by the use of the fun keyword, followed by the name of the function, parenthesis and the body of the function comes last which is introduced by the use of the curly braces.

    fun sayHello() {
        println("Hello there! Hope you are  having a good day?")
    }

    // Below we invoke/call our function
    sayHello()

    // Create your own example of a function below and also invoke it
    fun message() {
        println("Good afternoon. The meeting has been potsponed to Tuesday next week.")
    }

    message()
}